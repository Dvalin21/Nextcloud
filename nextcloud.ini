upload_max_filesize = 16G # Adjust as needed, default is often too low
post_max_size = 16G     # Must be equal to or greater than upload_max_filesize
memory_limit = 2048M     # Or higher, e.g., 1G if you have enough RAM
max_execution_time = 360 # Increase from default (e.g., 30s or 60s)
max_input_time = 360    # Increase from default
